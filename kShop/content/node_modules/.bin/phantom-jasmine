#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../phantom-jasmine/bin/phantom-jasmine" "$@"
  ret=$?
else 
  node  "$basedir/../phantom-jasmine/bin/phantom-jasmine" "$@"
  ret=$?
fi
exit $ret
